/*
スプレッド構文に慣れよう
*/

'use strict';

{
  const otherScores = [10, 20];
  const scores = [80, 90, 40, 70];
  //scoresの末尾にotherScoresの要素を追加する

  function sum(a, b) {
    console.log(a + b);
  }
  //sum()という関数を使ってotherScoresの要素の合計を出す

}