// Q.変数とは ?
// A.データを保存しておくための箱
// →可読性を上げるためにも大事


const foo = document.getElementById("foo");
const foodWidth = foo.offsetWidth;
const foodHeight = foo.offsetHeight;
const fooArea = foodWidth * foodHidth;
